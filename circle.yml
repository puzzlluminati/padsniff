machine:
  python:
    version: 3.5.2

dependencies:
  pre:
    - sudo apt-get install -y make pandoc

test:
  override:
    - make test

deployment:
  release:
    tag: /v[0-9]+(\.[0-9]+)*/
    commands:
      - make publish
